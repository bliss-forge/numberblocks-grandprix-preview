# Numberblocks Minigame Claude Instructions

## 프로젝트 목적

어린이가 숫자블록 친구와 함께 세기, 덧셈, 뺄셈, 곱셈, 교통안전 길찾기를 익히는 한국어·영어 교육용 웹 게임이다. 사용자는 게임 규칙과 음성에는 만족하며, Claude에게 후속 홈 화면 시각 리디자인을 맡길 예정이다.

## 현재 리디자인 작업 범위

홈의 게임 선택 화면만 시각적으로 개선한다. 카드 배치, 여백, 장식, 반응형 구성과 시각적 위계는 바꿀 수 있지만 게임 동작과 아래 계약은 유지한다.

## 변경할 수 없는 홈 카드 계약

| 번호 | 모드 | 제목 | 캐릭터 자산 |
| --- | --- | --- | --- |
| 1 | `count` | 몇 개일까? | `assets/characters/one.png` |
| 2 | `add` | 더하기 합체 | `assets/characters/two.png` |
| 3 | `sub` | 빼기 블록 | `assets/characters/three.png` |
| 4 | `mul` | 곱하기 블록 | `assets/characters/four.png` |
| 5 | `safety` | 안전한 길찾기 | `assets/characters/five.png` |
| 6 | `subway` | 지하철 여행 | `assets/characters/six.png` |
| 7 | `ktx` | 칙칙폭폭 기관사 | `assets/characters/seven.png` |

- 카드 순서는 1→7이며 1~6은 `aria-keyshortcuts`도 같은 숫자를 유지한다.
- 7번 카드는 숫자키가 없다(7·8·9는 난이도 키). 진입은 ←/→ 카드 포커스 이동
  + Space, 또는 클릭·터치. 카드 번호와 캐릭터 번호(7=seven.png)는 일치한다.
- 한국어 제목과 부제는 사용자의 명시적 승인 없이 바꾸지 않는다.
- 카드 번호와 캐릭터 번호는 항상 일치해야 한다.

## 시각 방향

- 현재 게임과 어울리는 밝고 장난감 같은 파스텔 분위기를 유지한다.
- 캐릭터는 평면 2D 스타일을 유지하고 3D 렌더링이나 입체 재해석을 하지 않는다.
- `assets/characters/`의 기존 캐릭터 이미지와 비율을 그대로 사용한다. 새 이미지 생성이나 자산 교체는 사용자가 별도로 요청한 경우에만 한다.
- PC `1280×720`에서는 다섯 카드를 한눈에 비교할 수 있어야 한다.
- 모바일 `390×844`에서는 가로 스크롤 없이 카드와 텍스트가 잘리지 않아야 한다.
- 키보드 번호 안내, 명확한 포커스 상태, 최소 `44×44px` 터치 영역을 유지한다.
- 과도한 그림자, 유리 효과, 보라색 중심의 일반적인 AI 대시보드 스타일은 사용하지 않는다.

## 보호 대상 기능

아래 영역은 홈 시각 리디자인에서 수정하지 않는다.

- 수학 문제 생성, 난이도와 숫자 범위
- 한국어·영어 음성, 효과음과 음소거 동작
- 숫자 입력, 모바일 숫자 패드와 키보드 입력
- 숫자블록 캐릭터 파일과 게임 중 캐릭터 배율
- 안전한 길찾기 지도, 신호등, 자동차와 장애물 로직
- 점수, 정답·오답과 화면 전환 동작

## 주요 파일

- `index.html`: 홈 카드 구조와 문구
- `styles.css`: 홈 및 게임의 모든 시각 스타일
- `assets/characters/`: 승인된 캐릭터 자산
- `src/app.mjs`: 게임 상태와 입력 처리, 홈 리디자인에서는 변경 금지
- `tests/app-contract.test.mjs`: 홈 카드 번호·모드·캐릭터 계약
- `tests/responsive-layout.test.mjs`: 반응형 스타일 계약

## 필수 작업 절차

1. 변경 전에 관련 HTML, CSS와 테스트를 읽고 기능 경계를 확인한다.
2. 기능 브랜치 또는 격리 worktree에서 작업한다.
3. 동작 변경이 필요하면 실패하는 테스트를 먼저 작성한다. 시각 변경은 기존 계약 테스트를 보존한다.
4. `npm test`를 실행해 실패가 0개인지 확인한다.
5. 홈 화면을 `1280×720`과 `390×844`에서 캡처한다.
6. 두 화면에서 잘림, 가로 스크롤, 번호·캐릭터 불일치와 포커스 표시를 확인한다.
7. 변경 파일, 테스트 수와 스크린샷을 사용자에게 보여주고 승인받는다.
8. 병합, GitHub 푸시와 Pages 배포는 사용자에게 영향 범위를 알리고 명시적으로 승인받은 뒤 진행한다.
